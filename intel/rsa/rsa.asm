section .data
section .bss
section .text
global _start
_start:
    ; greet 

    ; get user input
    ; implement arbitrary precision operations in library64

    ; calculate n = pq, phi(n)
    ; choose / prompt for e which is coprime with phi(n)
    ; check if e is its own inverse mod phi(n)
    ; compute d multiplicative inverse of e mod phi(n)
    
    ; public key = (e, n), private key = (d, n)
    ; print out public key, private key

    EXIT 0